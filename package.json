{
    "name": "caching",
    "description": "In-memory / haredis-based cache with smooth API for node.js",
    "version": "1.0.0",
    "author": "Vladimir Petriko inspired by Mathias Pettersson <mape@mape.me>",
    "engines": [
        "node"
    ],
    "directories": {
        "lib": "./lib"
    },
    "main": "./lib/caching",
    "repository": [
        {
            "type": "git",
            "url": "http://github.com/Targetprocess/node-caching"
        }
    ],
    "dependencies": {
        "haredis": ">=0.2.15"
    },
    "devDependencies": {
        "mocha": "1.20.1",
        "assert": "1.1.1"
    },
    "scripts": {
        "test": "mocha --reporter spec test/"
    }
}